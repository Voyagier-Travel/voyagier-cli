# voyagier

Convenience alias so that `npm install -g voyagier` and `npx voyagier` just work.

**The canonical package is [`@voyagier/cli`](https://www.npmjs.com/package/@voyagier/cli)** — this package is a thin wrapper that depends on `@voyagier/cli@^3` and delegates the `voyagier` bin to it. All documentation, versioning, and release notes live there.

```bash
npm install -g voyagier
voyagier --help
```

Need to pin an exact CLI version? Install the canonical package directly:

```bash
npm install -g @voyagier/cli@<version>
```

Book real travel from your terminal — search flights, hotels & activities, plan trips, and check out with a price-gated booking. For AI agents and travel advisors.

- Docs & source: https://github.com/Voyagier-Travel/voyagier-cli
- Issues: https://github.com/Voyagier-Travel/voyagier-cli/issues
