#!/usr/bin/env node
// Convenience alias: `npm install -g voyagier` / `npx voyagier`.
// The canonical package is @voyagier/cli — this delegates to its CLI entry.
import "@voyagier/cli/dist/index.js";
